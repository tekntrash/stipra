import 'package:flutter/material.dart';

class TradingPointOffersList extends StatelessWidget {
  const TradingPointOffersList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
